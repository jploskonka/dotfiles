# rm with prompt
alias rm='rm -i'
# cd 
alias ..='cd ..'

# ls
alias ls='pwd; ls -GF'
alias ls.='pwd; ls -aGF'
alias ll='pwd; ls -lGFh'
alias ll.='pwd; ls -alGFh'

# grep
alias grep='grep --color=auto'

# bc without welcome message and with math lib support
alias bc='bc -lq'

# colorful tree
alias t='tree -C -I "bundle|tmp|node_modules"'
alias td='tree -C -d' # directories only

# bundler
alias b='bundle exec'
alias bi='bundle install'
alias bcl='bundle clean'

# cat with syntax hihlighting :D 
alias ccat='pygmentize'

# one/two letter commands for often used commands
alias v='vim'
alias v.='vim .'
alias j='jobs'

# rails commands shortcuts
alias brs='bundle exec rails s -b 0.0.0.0'
alias pbrs='SHOW_PROFILER=1 brs'
alias brc='bundle exec rails c'
alias brm='bundle exec rake db:migrate'
alias br='bundle exec rails'
# spin
alias ss='spin serve'

# chrome-cli
alias ch='chrome-cli'
alias cho='chrome-cli open'
alias chi='chrome-cli info'

# tmux
alias tx='tmux'
alias txn='tmux new -s'
alias txl='tmux ls'
alias txa='tmux attach -t'

# use gnu date instead of date
alias date='gdate'

# node & npm
alias n='node'
alias ni='npm install'
alias nis='npm install --save'
alias nisd='npm install --save-dev'
alias np='npm prune'
alias nr='npm run'
alias nrd='npm run dev'
alias nt='npm test'

# elasticsearch
alias es='elasticsearch'

# presentations from markdown
alias ds='open -a /Applications/Deckset.app'
